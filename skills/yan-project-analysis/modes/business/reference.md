# Business document reference

只在证据足以生成持久业务文档时读取。以下是可选信息结构，不是固定章节清单。

## 最小骨架

```markdown
# <业务名> 业务地图

> 状态：已证实 / 草稿待确认
> 范围：已覆盖入口；缺失或排除入口
> 关键证据：代码、接口、配置、日志或文档位置

## 业务概述与可观察结果
[用测试/产品语言说明谁触发、要达成什么、成功或失败能看到什么]

## 流程与规则
[用最清楚的组合呈现：短文、决策表、业务流图、时序图或状态图]

## 数据与副作用
[仅列与目标相关的表/字段、缓存、消息、外部调用和核对点；纯查询可写无持久化副作用]

## 测试口径
| 场景 | 前置与操作 | 预期 | 核对证据 |
|---|---|---|---|

## 证据边界
- 已证实：...
- 假设：...
- 冲突：...
- Blocker / 待确认：...
```

## 按需维度

- 多角色或多渠道：补角色、入口、权限和上下文差异。
- 有明确状态字段：补允许/禁止转换、终态和失败状态。
- 跨服务、异步或回调：补调用时序、重试、幂等和最终一致性观察点。
- 多数据对象：按阶段说明读写、关键字段变化和测试核对方式。
- 契约变化：说明请求、响应、错误行为或副作用对测试的影响。

不要为完整感填空章节。每个结论应能回到证据或被明确标为假设。

## 可选发布

仅在已授权发布看板时读取 [共享看板发布流程](../../../_shared/board-publish-flow.md)。条目先写到 `project-html/data/_entry.json`，再由 `board-add.js` 处理；禁止手工插入标记行或整体重写 `data/changes.js`。
