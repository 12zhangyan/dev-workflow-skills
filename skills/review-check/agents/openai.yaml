interface:
  display_name: "Review Check"
  short_description: "按统一清单执行只读代码审查"
  default_prompt: "Use review-check skill to perform a read-only code review from a review task, dev-doc, patch, or diff."

policy:
  allow_implicit_invocation: true
