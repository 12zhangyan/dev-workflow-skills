---
name: yan-conversation-handoff
description: 基于当前对话和已取得证据，生成供下一段 AI 对话继续执行的精简移交文档。用户要求“写移交文档”“给下一个对话交接”“总结当前对话供另一个 AI 继续”“生成 handoff”时使用；不替代开发方案、项目分析或代码审查。
---

# Conversation handoff

## 目标

让下一位 Agent 不重做已完成工作，也不把摘要误当事实。移交只保留恢复任务所需的目标、当前状态、证据索引、未决边界和最小可执行动作集；不复述聊天历史，不强制接手方走固定 Skill 或流程。

默认保存到 `docs/handoffs/<日期>/<任务>-handoff.md`。用户只要聊天输出或当前项目不适合写文件时，直接返回 Markdown 并明确 `artifacts: 无`。不要登记 HTML 看板。

## 成功标准

- 明确原始目标、当前授权范围以及完成、部分完成、未开始或阻塞的真实状态。
- 区分当前证据已证实、仅由对话声明但未复核、合理推断和待确认；不把历史“完成”升级成当前已验证。
- 记录实际改动、VCS 状态、验证命令/结果和失败边界；未运行就写未运行。
- 给出按信息价值排序的最小证据索引。接手方先读最能裁决下一动作的材料，再按缺口扩展，不设固定文件数量。
- 接手提示包含目标、禁止越界事项、首要核验和必要动作；互不覆盖的动作可以并行，有依赖时只保留可立即推进的前沿。
- 有在途 Agent 或任务时，按需记录所有者/句柄、范围、当前状态、等待条件和已返回结果，避免重复派工。

## 自主取证

优先使用当前对话已经读取的文件和命令结果；只对容易漂移且能低成本核验的工作区、VCS 或产物状态做只读复核。不要为了“完整”擅自运行全量构建、外部调用或其他耗时验证。

证据冲突、移交范围歧义或目标文件冲突会改变接手路径时，才读取 [交互策略](../_shared/interaction-policy.md) 并问一个问题。未决的业务/API/权限/数据库问题通常正是需要移交的内容：仍生成 `NeedsConfirmation` 文档，只把下一动作限制为确认或补证据。非交互/无人值守运行中不等待提问；只有任务范围本身无法识别时才阻止生成交接内容。

不得在文档或复制提示中转录 API key、密码、token、cookie、私钥、完整连接串、个人信息或敏感业务值。只记录脱敏位置、配置键、风险与授权后的轮换动作。数据库只读；未获授权的 DDL/DML 只能记为待确认或 DBA 申请建议。

## 交付

准备生成完整文档时才读取 [精简模板](reference.md)。

准备落盘时先以当前 Skill 目录解析 [workflow-fs.js](../_shared/scripts/workflow-fs.js) 的绝对路径，用它创建日期目录并检查目标 `file-state`。目标不可读、状态未知或未获覆盖授权时不得覆盖；用户已明确授权覆盖精确目标时，核验目标后执行，不重复确认。默认生成路径撞名且用户未锁定文件名时，可以选择稳定时间/版本后缀并报告实际路径。不得自动 add、commit 或 push。

用户或下游契约明确要求标准 `Workflow Brief` 时，才读取 [共享 Brief 协议](../_shared/workflow-brief.md)，在移交文档内附一份；普通 handoff 不重复这套字段。

最终只报告移交状态、文档路径或未写原因，以及可复制的接手提示。接手方先核验高价值证据，不凭摘要猜业务、权限、接口、数据库或部署状态。
