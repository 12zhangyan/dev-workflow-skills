interface:
  display_name: "Review Fix"
  short_description: "生成多 AI 审查任务包与修复交接"
  default_prompt: "Use review-fix skill to create a distributable AI review task package and later summarize fixes. Enforce a minimum evidence gate; distinguish plan review from implementation review, and if diff/status/changed files or business decisions are missing, stop and list blockers."

policy:
  allow_implicit_invocation: true
